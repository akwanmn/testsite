= render 'admin/users/shared/masthead'
= simple_form_for admin_users_path, method: :get, html: {class: 'form form-search'} do |f|
  %input.input-medium.search-query{type: 'text', name: 'search'}
  %button.btn{type: 'submit'} Search
  = link_to 'Reset', admin_users_path, class: 'btn'
  .pull-right
    = link_to 'Create User', '#', class: 'btn btn-primary'
%table.table.table-bordered.table-striped.table-highlight
  %thead
    %tr
      %th Name
      %th E-Mail
      %th Gender
      %th Birthday
      %th Age
      %th Status
      %th Functions
  %tbody
    - @users.each do |u|
      %tr
        %td= u.full_name
        %td
          =link_to u.email, '#'
        %td= t(u.user_profile.gender)
        %td= l(u.user_profile.birthday)
        %td= u.user_profile.age
        %td
          %span.label{class: "#{u.membership_status.downcase}"}= u.membership_status
        %td
          - if u.deleted_at
            = link_to 'Restore', restore_admin_user_path(u.id), class: 'btn btn-success'
          - else
            = link_to 'Edit', edit_admin_user_path(u), class: 'btn btn-secondary'
            = link_to 'Delete', disable_admin_user_path(u), class: 'btn btn-danger msgbox-confirm',
              confirm: "Delete #{u.full_name}?"
            /= link_to 'Switch', "/switch_user?scope_identifier?user_#{u.id}", class: 'btn btn-tertiary'
            = link_to 'Orders', admin_user_orders_path(u), class: 'btn btn-primary'
            = link_to 'Profile', edit_admin_user_user_profile_path(u), class: 'btn btn-tertiary'

= paginate @users, window: 2