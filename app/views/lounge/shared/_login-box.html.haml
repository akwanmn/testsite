.login.pull-right
  = simple_form_for(User.new, url: new_user_session_path, html: {class: 'navbar-form'}) do |f|
    = f.text_field :nickname, placeholder: 'Nickname', class: 'input-medium'
    = f.password_field :password, placeholder: 'Password', class: 'input-medium'
    = f.submit :submit, class: 'btn btn-custom2', value: 'Login'
    .options
      /- if user_signed_in?
      /  = link_to 'Logout', destroy_user_session_path, :method => :delete
      /  = current_user.full_name
      %label.checkbox
        = f.check_box :remember_me
        Keep me logged in.
        /= "|"
        /= link_to 'Forgot Password', '#'