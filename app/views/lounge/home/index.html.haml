!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}/
    %title 2Date4Love - A different kind of intimacy.
    %meta{:content => "", :name => "description"}/
    %meta{:content => "", :name => "author"}/
    = stylesheet_link_tag 'lounge', 'lounge/home'
    %link{href:'http://fonts.googleapis.com/css?family=Petit+Formal+Script', rel: 'stylesheet', type: 'text/css'}
    %link{href:'http://fonts.googleapis.com/css?family=Oxygen', rel: 'stylesheet', type: 'text/css'}


    /[if gte IE 9]
      :css
        .gradient {
          filter: none;
        }
    /[if lte IE 8]
      :css
        .background-blur {
          background: transparent;
          filter:progid:DXImageTransform.Microsoft.gradient(startColorstr=#7FFFFFFF,endColorstr=#7FFFFFFF);
          zoom: 1
        }
    /[if lt IE 9]
      %script{src: '/assets/html5shiv.js'}
  %body{'cz-shortcut-listen' => true}
    #wrap
      = render 'lounge/shared/topnav'
      .sample-background
        .container
          .row
            = render 'lounge/shared/logo-text'
            - if @user.errors.any?
              .alert.alert-error
                = "#{pluralize(@user.errors.count, 'error')} were found."
              - @user.errors.full_messages.each do |error|
                %p= error
            .span5.background-blur
              .span5.join-form
                .join-today Join Today
                = simple_form_for @user, url: lounge_signup_path, wrapper: :small do |f|
                  .controls.controls-row
                    = f.simple_fields_for :user_profile do |up|
                      = up.input :first_name, input_html: {class: 'span2'}, placeholder: 'First Name'
                      = up.input :last_name, input_html: {class: 'span2'}, label: false
                  = f.submit :submit
      = render 'lounge/shared/promos'
      = render 'lounge/shared/footer'
      = javascript_include_tag 'main', 'lounge'