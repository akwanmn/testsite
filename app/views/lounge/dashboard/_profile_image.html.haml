- if image.present?
  %a.thumbnail{href: "#{image.url}"}
    %img{src: "#{image.thumb.url}"}
- else
  %a.thumbnail{href: '#', :style => 'width: 130px; height: 109px'}
    %img{src: '/assets/lounge/default-avatar.jpg', width: '130px', height: '109px'}
